# bool и char (Boolean & Character)

[← Дробные числа](03-floats.md) | [Оглавление](README.md) | [Составные типы →](05-compound.md)

---

## bool — логический тип (Boolean)

**bool** (от **Boolean**, булев тип) хранит только два значения:

```
true  = да, правда, включено
false = нет, ложь, выключено
```

### Примеры

```rust
let is_student: bool = true;   // да
let is_working: bool = false;  // нет
```

### Для чего?

```rust
let age = 20;
let can_vote: bool = age >= 18;  // true

if can_vote {
    println!("Можно голосовать!");
}
```

---

## char — символ (Character)

**char** (от **character** — символ) хранит ОДНУ букву, цифру или символ.

### Примеры

```rust
let letter: char = 'A';
let digit: char = '7';
let emoji: char = '😀';
let russian: char = 'Я';
```

### Важно: одинарные кавычки!

```rust
'A'   // char — один символ
"A"   // строка — текст (это другой тип!)
```

---

## Сравнение

| Тип | Размер | Значения | Пример |
|-----|--------|----------|--------|
| `bool` | 1 байт | `true`, `false` | `let ok = true;` |
| `char` | 4 байта | Любой символ | `let c = 'A';` |

---

[← Дробные числа](03-floats.md) | [Оглавление](README.md) | [Составные типы →](05-compound.md)

# День 0 — Фундамент перед изучением Rust

> **Цель:** Понять базовые концепции, которые делают Rust уникальным. Без этого материала Day 1 будет сложнее.

---

## Зачем нужен Day 0?

Rust отличается от других языков. Чтобы понимать **почему** Rust делает что-то определённым образом, нужно знать:

- Как работает память компьютера
- Что такое типы данных
- Какие проблемы решает Rust

**Этот материал — инвестиция.** Потратив час сейчас, сэкономишь дни позже.

---

## Содержимое каталога

| Файл | Тема | Время чтения |
|------|------|--------------|
| `why-rust.md` | Почему Rust уникален, сравнение с другими языками | 10 мин |
| `memory.md` | Как работает память: стек, куча, владение | 15 мин |
| `types.md` | Типы данных: числа, строки, составные типы | 15 мин |

---

## Порядок изучения

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  1. why-rust.md    →  Зачем учить Rust? Мотивация          │
│         ↓                                                   │
│  2. memory.md      →  Стек, куча, владение (ключевое!)     │
│         ↓                                                   │
│  3. types.md       →  Типы данных в Rust                   │
│         ↓                                                   │
│  4. Day 1          →  Переменные и связывание              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## Ключевые концепции (preview)

### Память

```
СТЕК (Stack)              КУЧА (Heap)
─────────────             ───────────
Быстрый                   Медленный
Маленький                 Большой
Автоматический            Управляемый
Фиксированный размер      Динамический размер

i32, bool, char           String, Vec, Box
```

### Владение (Ownership)

```rust
let s1 = String::from("hello");
let s2 = s1;  // s1 перемещается в s2

// s1 больше нельзя использовать!
```

**Это уникальная фича Rust** — безопасность памяти без сборщика мусора.

### Типы

```rust
// Целые числа
let x: i32 = 42;      // знаковое 32-бит
let y: u8 = 255;      // беззнаковое 8-бит

// Дробные
let pi: f64 = 3.14;

// Логический
let active: bool = true;

// Символ
let emoji: char = '🦀';

// Составные
let point: (i32, i32) = (10, 20);  // кортеж
let nums: [i32; 3] = [1, 2, 3];    // массив
```

---

## Проверь себя

После прочтения материалов ты должен уметь ответить:

1. **Чем стек отличается от кучи?**
2. **Почему `String` хранится в куче, а `i32` на стеке?**
3. **Что такое владение (ownership)?**
4. **Почему Rust быстрый как C, но безопасный как Python?**
5. **Что означает `let x: i32 = 5;`?**

Если не можешь — перечитай материалы. Это нормально!

---

## Советы по изучению

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  • Не торопись. Лучше понять, чем запомнить.               │
│                                                             │
│  • Рисуй схемы. Визуализация помогает понять память.       │
│                                                             │
│  • Возвращайся сюда. Когда что-то непонятно в Day 1+,      │
│    перечитай Day 0.                                         │
│                                                             │
│  • Компилятор — друг. Его ошибки — это обучение.           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## Готов?

```
Day 0: Фундамент     ✓ Ты здесь
       ↓
Day 1: Переменные    → binding.md, variables.rs
       ↓
Day 2: ...
```

Переходи к чтению материалов!

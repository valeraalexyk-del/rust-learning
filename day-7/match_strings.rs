fn main() {                               // начало программы
    let command = "start";                // строковая команда

    match command {                       // проверяем значение строки
        "start" => println!("Запуск!"),   // если команда "start"
        "stop" => println!("Остановка!"), // если команда "stop"
        "help" => println!("Помощь!"),    // если команда "help"
        _ => println!("Неизвестная команда"), // иначе
    }
}

/*
Зачем это нужно:
- match отлично подходит для обработки текстовых команд.
- Это часто используется в CLI-программах (программы в терминале).

Где применяется:
- обработка пользовательских команд
- меню в программах
- обработка ввода
*/
